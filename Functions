===========================================================Functions=============================================================

CREATE FUNCTION "PRACTICE_BATCHONE"."SIMPLEONE"(VAL1 INTEGER, VAL2 INTEGER)
RETURNS OUT_VALE INTEGER
AS
BEGIN
OUT_VALE := :VAL1+:VAL2;
END;

select "PRACTICE_BATCHONE"."SIMPLEONE"(10,30) from dummy

SCALAR FUNCTIONS will be used in the select column in the Query .
Table function will be used in the from clause of the SQL Query.
